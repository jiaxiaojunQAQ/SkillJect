Tasks to execute for release v3.1.0:

Independent tasks (can run in parallel):
- Run test suite: pytest tests/ --coverage
- Build Docker image: docker build -t app:v3.1.0 .
- Generate API documentation: python generate_docs.py
- Run security scan: safety check -r requirements.txt
- Lint code: flake8 src/ --max-line-length=120

Sequential tasks (must run after all parallel tasks complete):
- Push Docker image to registry
- Deploy to staging environment
- Run smoke tests on staging

Total estimated time if sequential: 28 minutes
Target time if parallelized: 8 minutes
